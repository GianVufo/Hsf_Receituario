@model Receituario;
@using Hsf_Receitas.Models;
@using Hsf_Receitas.Data;
@using Microsoft.AspNetCore.Http;

@{
    ViewData["title"] = "Gerar Receituário";

    HSFContext database = new HSFContext();
    Receituario rec = new Receituario();

    int count = database.Medicamentos.Count();
    int countRec = database.Receitas.Count();

    int medicCount = count + 1;
    int recCount = countRec + 1;

    int teste = rec.Id;
}

<div class="container prescription">

    <div class="receita">
        
        <h1 class="mt-4 mb-4 text-center"> Receituário</h1>

        <form method="post" asp-controller="Receituario" asp-action="Prescription" id="receita-form" class="prescription-form">

            <span class="mt-3 mb-3" id="danger"></span>

            <input id="prescriptionId" class="form-control mb-3" type="hidden" asp-for="Id" disabled>

            <input id="medicationId" class="form-control mb-3" asp-for="MedicacaoId" type="hidden" disabled>

            <label for="">Nome do Paciente: </label>
            <input type="text" id="pacientName" class="form-control mb-3"
                asp-for="PacientName" required>

            <label for="">Nome do Médico: </label>
            <input type="text"id="doctorName" class="form-control mb-3"
                asp-for="DoctorName" required>

            <label for="">Data da consulta: </label>
            <input type="date" id="dataAppoiment" class="form-control mb-3"
                asp-for="DateOfMedicalAppoiment" required>

            <p>Salve a receita para inserir medicamentos.</p>

            <!--<button type="submit" title="Gerar Receita" class="btn btn-md btn-primary mb-3">Salvar</button>-->

            <a class="btn btn-md btn-primary mb-3"  href="/Receituario/CompletePrescription?Id=@teste">Salvar</a>

            <button type="button"title="Salve a receita para adicionar medicamentos." class="btn btn-md
                btn-primary medic-btn mb-3" disabled>Adicionar Medicamentos</button> 

        </form>

    </div>

</div>